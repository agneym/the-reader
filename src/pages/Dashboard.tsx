import React, { FC, Fragment } from "react";

import Navbar from "../components/Navbar";

const Dashboard: FC = () => {
  return (
    <Fragment>
      <Navbar />
    </Fragment>
  );
};

export default Dashboard;
